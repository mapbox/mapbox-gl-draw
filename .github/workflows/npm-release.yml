name: NPM release
on:
  push:
    tags:
      - v*
jobs:
  npm-release:
    uses: mapbox/gha-public/.github/workflows/workflow-npm-oidc-publish.yml@main
    permissions:
      id-token: write
      contents: write
    with:
      environment: npm-release